# TribeOS web environment (example). Copy to `.env.local` for local development.
# Only NEXT_PUBLIC_* variables are exposed to the browser.

# Base URL of the TribeOS backend API (default uvicorn: port 8000).
# Do not point this at unrelated local services on other ports.
NEXT_PUBLIC_API_URL=http://localhost:8000
